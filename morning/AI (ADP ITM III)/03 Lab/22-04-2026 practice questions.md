Here are 30 code-based questions ranging from basic to advanced, focusing strictly on **Input/Output, Arithmetic, Escape Characters, and String Formatting.**

### Basic Level (1-10)

1. Write a code to take two numbers as input from the user and print their sum.
2. Write a single `print` statement that outputs: `Apple-Orange-Banana` using three separate variables and the `sep` parameter.
3. Use the `end` parameter in `print()` to make two separate print statements appear on the same line with a space between them.
4. Write a script that takes a string as input and prints it 10 times repeated without using a loop.
5. What is the output of `print(2 ** 3 + 5)`?
6. Write code to take a float input and print only the integer part of that number using an arithmetic operator.
7. Correct the following code to make it run:
   ```python
   age = input("Enter age: ")
   print("In 5 years you will be " + age + 5)
   ```
8. Write a code to print the following exact string: `He said, "Python is amazing!"` (Include the double quotes).
9. Write a script that calculates the remainder when `145` is divided by `12`.
10. Write code that takes a name as input and prints `Hello [name]` using the `+` operator.

### Intermediate Level (11-20)

11. Write a `print` statement using the `\t` and `\n` escape characters to display a "Name" and "Age" in a table-like format.
12. Use the `.format()` method to print: `The coordinates are 10.5 and 20.3` using the variables `x = 10.5` and `y = 20.3`.
13. Write a script that takes a radius as input and calculates the area of a circle ($\pi r^2$). (Use `3.14` for $\pi$).
14. Create an f-string that displays the value of `pi = 3.14159265` rounded to exactly 3 decimal places.
15. What is the output of `print(10 // 3, 10 % 3)`?
16. Write a code using a **Raw String** to print this file path: `C:\users\name\desktop`.
17. Using the `.format()` method with index numbers (e.g., `{0}`), print "Bread and Jam" by passing the arguments in reverse order (`"Jam"`, `"Bread"`).
18. Write a code that takes a user's name and prints it right-aligned within 20 characters of space using f-strings.
19. What is the result of `print("Python" * (10 // 3))`?
20. Write code to print the character `\` without using a raw string.

### Advanced Level (21-30)

21. What is the output of `print(-7 // 2)`?
22. Write an f-string that takes the number `1000000` and prints it with commas as thousands separators (e.g., `1,000,000`).
23. Write a code that takes a decimal input (like `0.75`) and prints it as a percentage (e.g., `75%`) using f-string formatting.
24. Explain the output of the following code:
    ```python
    print(5 + 2 * 3 ** 2)
    ```
25. Write a script that takes a large integer and prints its **Binary** and **Hexadecimal** equivalents using f-string format specifiers.
26. How do you print the literal string `{{Hello}}` using an f-string without it being interpreted as a variable placeholder?
27. What is the output of `print(10 % -3)`?
28. Use f-string formatting to print the number `42` padded with zeros so it is 5 digits long (e.g., `00042`).
29. Write a single `print` statement that uses `\r` (carriage return) to write "Hello World" but then replaces "Hello" with "Super" resulting in "Super World".
30. Given `a = 5` and `b = 10`, write a single line of code using an arithmetic trick to swap their values without using a third variable. (Print the final results).